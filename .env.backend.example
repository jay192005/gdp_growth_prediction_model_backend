# Backend Environment Variables Example
# Copy this file to .env for local development

# Port (optional, defaults to 5000)
PORT=5000

# Python version (optional)
PYTHON_VERSION=3.11.0

# Firebase Credentials (for production only)
# For local development, use firebase_credentials.json file instead
# For Render deployment, paste the entire JSON content here
# FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your-project",...}
